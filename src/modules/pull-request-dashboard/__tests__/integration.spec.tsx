import React from 'react';
import { PullRequestsDashboard } from '../index';

describe('Pull requests dashboard integration tests', () => {
    it('Order page functional component should exist', () => {
        expect(PullRequestsDashboard).toBeDefined();
    })
})
